# Module specs

A module gets a file here only when it is scheduled to be built. Each spec says: what the engineer sees, what the agent proposes, what the code checks, what is stored, and how it is tested. No spec, no build.
